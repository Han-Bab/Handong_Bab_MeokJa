import 'package:flutter/material.dart';

class AfterGoogleLogin extends StatelessWidget {
  const AfterGoogleLogin({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
